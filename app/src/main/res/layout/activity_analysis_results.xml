<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    tools:context=".AnalysisResultsActivity">

    <!-- Прокручиваемая область: предупреждение и результаты -->
    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/btnDetails"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="32dp"
            android:paddingBottom="24dp">

            <!-- Предупреждение — покрупнее -->
            <TextView
                android:id="@+id/tvWarning"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/warning_not_diagnosis"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="#D32F2F"
                android:gravity="center"
                android:lineSpacingExtra="6dp" />

            <!-- Надпись "Вероятность наличия болезни Паркинсона" — по центру, синим -->
            <TextView
                android:id="@+id/tvProbabilityLabel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:layout_marginTop="48dp"
                android:text="@string/probability_label"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="#2196F3" />

            <!-- Проценты -->
            <TextView
                android:id="@+id/tvProbability"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="24dp"
                android:text="0.0%"
                android:textSize="48sp"
                android:textStyle="bold"
                android:textColor="#2196F3" />
        </LinearLayout>
    </ScrollView>

    <!-- Кнопка Подробнее — побольше -->
    <Button
        android:id="@+id/btnDetails"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="16dp"
        android:text="@string/btn_details"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#2196F3"
        android:backgroundTint="#E3F2FD"
        android:padding="28dp"
        android:minHeight="80dp"
        android:elevation="2dp"
        android:stateListAnimator="@null"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toTopOf="@id/btnRepeat" />

    <!-- Кнопки привязаны к нижнему краю, побольше -->
    <!-- Кнопка Повторить -->
    <Button
        android:id="@+id/btnRepeat"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="16dp"
        android:text="@string/btn_repeat"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:backgroundTint="#2196F3"
        android:padding="28dp"
        android:minHeight="80dp"
        android:elevation="4dp"
        android:stateListAnimator="@null"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toTopOf="@id/btnFinish" />

    <!-- Кнопка Завершить -->
    <Button
        android:id="@+id/btnFinish"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp"
        android:text="@string/btn_finish"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:backgroundTint="#757575"
        android:padding="28dp"
        android:minHeight="80dp"
        android:elevation="4dp"
        android:stateListAnimator="@null"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
